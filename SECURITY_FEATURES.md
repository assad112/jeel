# 🔒 Bank-Level Security Features

## نظرة عامة

تم تطبيق **BankSecurityWrapper** في التطبيق لتوفير حماية أمنية شاملة بمستوى البنوك.

---

## 🛡️ الميزات الأمنية المُطبقة

### 1️⃣ **منع التقاط الشاشة (Screenshot Prevention)**
- ✅ منع التقاط الشاشة في Android و iOS
- ✅ منع تسجيل الشاشة (Android 11+)
- ✅ إخفاء المحتوى في App Switcher عند الانتقال للخلفية

**المكتبة المستخدمة:** `screen_protector`

```dart
await ScreenProtector.protectDataLeakageOn();
await ScreenProtector.preventScreenshotOn();
```

---

### 2️⃣ **كشف كسر الحماية (Jailbreak/Root Detection)**
- ✅ كشف الأجهزة المكسورة (Jailbroken/Rooted)
- ✅ كشف وضع المطور (Developer Mode)
- ✅ التحقق من أن الجهاز حقيقي وليس محاكي
- ✅ منع تشغيل التطبيق على الأجهزة غير الآمنة

**المكتبة المستخدمة:** `safe_device`

```dart
final isCompromised = await SecurityService.isDeviceCompromised();
```

---

### 3️⃣ **مهلة عدم النشاط (Inactivity Timeout)**
- ✅ مراقبة نشاط المستخدم
- ✅ تسجيل الخروج التلقائي بعد فترة من عدم النشاط
- ✅ المدة الافتراضية: 5 دقائق (قابلة للتخصيص)
- ✅ عرض رسالة تحذير قبل تسجيل الخروج

```dart
BankSecurityWrapper(
  inactivityTimeout: Duration(minutes: 5),
  onInactivityTimeout: () {
    // تسجيل الخروج أو إعادة طلب المصادقة
  },
  child: YourApp(),
)
```

---

### 4️⃣ **حماية الخلفية (Background Protection)**
- ✅ إخفاء المحتوى الحساس عند الانتقال للخلفية
- ✅ عرض شاشة "Secured" في App Switcher
- ✅ مراقبة حالة التطبيق (Foreground/Background)
- ✅ إيقاف مؤقت عدم النشاط عند الانتقال للخلفية

---

### 5️⃣ **معلومات الجهاز والأمان (Device Security Info)**
- ✅ الحصول على معلومات الجهاز
- ✅ التحقق من نسخة نظام التشغيل
- ✅ التحقق من أن الجهاز فيزيائي (ليس محاكي)
- ✅ طباعة تقرير أمني شامل

**المكتبة المستخدمة:** `device_info_plus`

```dart
final securityInfo = await SecurityService.getDeviceSecurityInfo();
```

---

## 📦 المكتبات المستخدمة

```yaml
dependencies:
  screen_protector: ^1.4.2    # منع التقاط الشاشة
  safe_device: ^1.1.8         # كشف كسر الحماية
  device_info_plus: ^10.1.2   # معلومات الجهاز
```

---

## 🚀 كيفية الاستخدام

### الاستخدام الأساسي

في `main.dart`:

```dart
import 'package:flutter/material.dart';
import 'widgets/bank_security_wrapper.dart';

void main() {
  runApp(
    MaterialApp(
      home: BankSecurityWrapper(
        child: YourApp(),
      ),
    ),
  );
}
```

### الاستخدام المتقدم (مع خيارات مخصصة)

```dart
BankSecurityWrapper(
  // تفعيل/تعطيل الميزات
  enableScreenshotProtection: true,
  enableJailbreakDetection: true,
  enableInactivityTimeout: true,
  
  // تخصيص مهلة عدم النشاط
  inactivityTimeout: Duration(minutes: 5),
  
  // عرض التحذيرات الأمنية
  showSecurityWarnings: true,
  
  // معالجة الأحداث
  onInactivityTimeout: () {
    print('User has been inactive');
    // تنفيذ منطق تسجيل الخروج
  },
  
  onSecurityViolation: () {
    print('Security violation detected!');
    // إرسال تنبيه للمسؤول
  },
  
  child: YourApp(),
)
```

---

## 🔍 فحص الأمان

### إجراء فحص أمني شامل

```dart
import 'services/security_service.dart';

final result = await SecurityService.performSecurityCheck(
  allowEmulator: false,        // منع المحاكي
  allowRootedDevices: false,   // منع الأجهزة المكسورة
  allowDeveloperMode: false,   // منع وضع المطور
);

if (result.isPassed) {
  print('✅ Device is secure');
} else {
  print('❌ Security check failed');
  print('Errors: ${result.errors}');
}
```

### طباعة معلومات الأمان

```dart
await SecurityService.printSecurityInfo();
```

**مثال على الناتج:**

```
🔒 Security Information:
══════════════════════════════════════════════════
  platform: Android
  version: 13
  sdk: 33
  brand: Samsung
  model: Galaxy S21
  isPhysicalDevice: true
  isCompromised: false
  isDeveloperMode: false
══════════════════════════════════════════════════
```

---

## 🎯 السيناريوهات المدعومة

### ✅ السيناريو 1: جهاز آمن
- الجهاز حقيقي (ليس محاكي)
- غير مكسور الحماية
- **النتيجة:** السماح بفتح التطبيق ✅

### ❌ السيناريو 2: جهاز مكسور الحماية
- الجهاز Jailbroken أو Rooted
- **النتيجة:** رفض فتح التطبيق وعرض رسالة خطأ ❌

### ⚠️ السيناريو 3: وضع المطور مفعّل
- وضع المطور مفعّل على Android
- **النتيجة:** عرض تحذير (يمكن السماح به في التطوير) ⚠️

### ⏰ السيناريو 4: عدم النشاط
- المستخدم لم يتفاعل لمدة 5 دقائق
- **النتيجة:** عرض رسالة تحذير وطلب إعادة المصادقة ⏰

---

## 🛠️ التخصيص

### تعطيل ميزة معينة

```dart
BankSecurityWrapper(
  enableScreenshotProtection: true,   // تفعيل
  enableJailbreakDetection: false,    // تعطيل
  enableInactivityTimeout: true,      // تفعيل
  child: YourApp(),
)
```

### تغيير مدة عدم النشاط

```dart
BankSecurityWrapper(
  inactivityTimeout: Duration(minutes: 3),  // 3 دقائق بدلاً من 5
  child: YourApp(),
)
```

### إضافة منطق مخصص عند انتهاء الجلسة

```dart
BankSecurityWrapper(
  onInactivityTimeout: () async {
    // 1. حفظ حالة التطبيق
    await saveAppState();
    
    // 2. تسجيل الخروج
    await logout();
    
    // 3. العودة لشاشة تسجيل الدخول
    Navigator.pushReplacementNamed(context, '/login');
  },
  child: YourApp(),
)
```

---

## 🧪 الاختبار

### اختبار منع التقاط الشاشة
1. شغّل التطبيق على جهاز حقيقي
2. حاول التقاط الشاشة
3. **النتيجة المتوقعة:** يظهر تنبيه أو شاشة سوداء

### اختبار كشف كسر الحماية
1. شغّل التطبيق على جهاز مكسور
2. **النتيجة المتوقعة:** رسالة خطأ ورفض فتح التطبيق

### اختبار مهلة عدم النشاط
1. شغّل التطبيق
2. لا تتفاعل لمدة 5 دقائق
3. **النتيجة المتوقعة:** رسالة تحذير انتهاء الجلسة

### اختبار حماية الخلفية
1. شغّل التطبيق
2. اضغط زر Home للانتقال للخلفية
3. افتح App Switcher
4. **النتيجة المتوقعة:** شاشة "Secured" بدلاً من المحتوى

---

## ⚠️ ملاحظات مهمة

### 🔴 في وضع الإنتاج (Production)
- ✅ جميع الفحوصات الأمنية مُفعّلة
- ❌ لا يُسمح بالمحاكي
- ❌ لا يُسمح بالأجهزة المكسورة
- ❌ لا يُسمح بوضع المطور

### 🟡 في وضع التطوير (Debug)
- ✅ تخفيف القيود للسماح بالتطوير
- ✅ السماح بالمحاكي
- ✅ السماح بوضع المطور
- ⚠️ طباعة التحذيرات الأمنية

### التحكم في السلوك

```dart
import 'package:flutter/foundation.dart';

final result = await SecurityService.performSecurityCheck(
  allowEmulator: kDebugMode,       // سماح في Debug فقط
  allowRootedDevices: false,       // منع دائماً
  allowDeveloperMode: kDebugMode,  // سماح في Debug فقط
);
```

---

## 📊 ملخص الحماية

| الميزة | الحالة | المنصة |
|-------|--------|--------|
| منع التقاط الشاشة | ✅ | Android, iOS |
| كشف كسر الحماية | ✅ | Android, iOS |
| مهلة عدم النشاط | ✅ | الكل |
| حماية الخلفية | ✅ | الكل |
| كشف المحاكي | ✅ | Android, iOS |
| كشف وضع المطور | ✅ | Android |

---

## 🔗 الملفات ذات الصلة

```
lib/
├── main.dart                           # تطبيق BankSecurityWrapper
├── widgets/
│   └── bank_security_wrapper.dart      # الغلاف الأمني الرئيسي
└── services/
    ├── security_service.dart           # خدمات الأمان
    ├── biometric_service.dart          # المصادقة بالبصمة
    └── secure_storage_service.dart     # التخزين الآمن
```

---

## 🎓 أفضل الممارسات

1. **دائماً فعّل `enableScreenshotProtection` في الإنتاج**
   ```dart
   enableScreenshotProtection: !kDebugMode,
   ```

2. **استخدم مدة معقولة لعدم النشاط (3-5 دقائق)**
   ```dart
   inactivityTimeout: Duration(minutes: 3),
   ```

3. **امسح البيانات الحساسة عند انتهاء الجلسة**
   ```dart
   onInactivityTimeout: () async {
     await SecureStorageService.clearAll();
   }
   ```

4. **أرسل تقارير الانتهاكات الأمنية للسيرفر**
   ```dart
   onSecurityViolation: () async {
     await reportSecurityViolation();
   }
   ```

---

## 📞 الدعم

للاستفسارات أو المشاكل الأمنية، يرجى التواصل مع فريق التطوير.

---

**تم التطبيق بنجاح! 🎉**

جميع ميزات الأمان تعمل الآن بشكل كامل في التطبيق.

