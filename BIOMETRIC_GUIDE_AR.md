# 👆 دليل استخدام البصمة في تطبيق Jeel ERP

## 📋 نظرة عامة

هذا الدليل يشرح لك **كيفية حفظ بيانات تسجيل الدخول وتفعيل البصمة** في تطبيق Jeel ERP.

---

## 🎯 الخطوات الكاملة

### **المرحلة 1: أول مرة - تسجيل الدخول اليدوي**

#### **الخطوة 1: فتح التطبيق**
```
1. افتح تطبيق Jeel ERP
2. ستفتح صفحة تسجيل الدخول تلقائياً
```

#### **الخطوة 2: تسجيل الدخول**
```
1. أدخل اسم المستخدم (Username)
2. أدخل كلمة المرور (Password)
3. اضغط على زر "Login" أو "تسجيل الدخول"
```

#### **الخطوة 3: بعد نجاح تسجيل الدخول** ✨
```
✅ سيتم اكتشاف نجاح تسجيل الدخول تلقائياً
✅ سيظهر لك حوار يسأل:
   "هل تريد حفظ بيانات تسجيل الدخول؟"
   
   الخيارات:
   - ✅ "نعم، احفظ" → سيحفظ البيانات
   - ❌ "لا، شكراً" → لن يحفظ
```

#### **الخطوة 4: تفعيل البصمة** 👆
```
بعد حفظ البيانات، سيظهر حوار آخر:
   "هل تريد تفعيل المصادقة بالبصمة؟"
   
   الخيارات:
   - ✅ "تفعيل" → يتم تفعيل البصمة
   - ❌ "ليس الآن" → يمكن تفعيلها لاحقاً
```

---

### **المرحلة 2: المرة القادمة - تسجيل الدخول بالبصمة**

#### **الطريقة 1: استخدام زر البصمة** 👆

```
1. افتح التطبيق
2. انتظر حتى تفتح صفحة تسجيل الدخول
3. ستجد زر كبير في الأسفل:
   ┌──────────────────────────┐
   │ 👆  Login with Biometric │
   └──────────────────────────┘
   
4. اضغط على الزر
5. ضع إصبعك على ماسح البصمة (أو Face ID)
6. ✅ سيتم ملء البيانات وتسجيل الدخول تلقائياً!
```

#### **الطريقة 2: من الإعدادات**

```
1. افتح التطبيق
2. اضغط على أيقونة ☰ (القائمة) في الأعلى
3. اختر "Settings" (الإعدادات)
4. اختر "Biometric Settings"
5. اضغط "Enable Biometric"
```

---

## 📱 السيناريوهات المختلفة

### **السيناريو 1: أول مرة - لا توجد بيانات محفوظة**

```
1. تسجيل دخول يدوي → ✅
2. يظهر سؤال: "احفظ البيانات؟" → ✅ نعم
3. يظهر سؤال: "تفعيل البصمة؟" → ✅ نعم
4. ✅ تم الحفظ والتفعيل!
```

### **السيناريو 2: البيانات محفوظة ولكن البصمة غير مفعلة**

```
1. افتح صفحة تسجيل الدخول
2. اضغط زر البصمة 👆
3. ستظهر رسالة ترحيبية
4. يمكنك:
   - تسجيل الدخول يدوياً
   - بعد النجاح → سيُسأل عن تفعيل البصمة
```

### **السيناريو 3: كل شيء مفعل ومحفوظ** ✨

```
1. افتح صفحة تسجيل الدخول
2. اضغط زر البصمة 👆
3. ضع بصمتك
4. ✅ تسجيل دخول تلقائي فوري!
```

---

## 🔧 طريقة تفعيل البصمة من الإعدادات

### **من داخل التطبيق:**

```
1. افتح التطبيق
2. اضغط على ☰ (القائمة) في AppBar
3. اختر "Settings"
4. في القائمة:
   ├── Manage Login Credentials (إدارة البيانات)
   └── Biometric Settings (إعدادات البصمة)
5. اضغط على "Biometric Settings"
6. ستجد:
   ✅ Biometric available: نعم/لا
   ✅ Biometric enabled: مفعّل/غير مفعّل
7. اضغط "Enable Biometric" لتفعيلها
```

---

## 📊 الحالات المختلفة

### **✅ الحالة 1: البصمة متوفرة ومفعّلة**

```
الزر يظهر: ✅ نعم
عند الضغط: طلب البصمة من النظام
بعد النجاح: تسجيل دخول تلقائي ✅
```

### **⚠️ الحالة 2: البصمة متوفرة ولكن غير مفعلة**

```
الزر يظهر: ✅ نعم
عند الضغط: رسالة ترحيبية
الخطوة التالية: تسجيل دخول يدوي → ثم تفعيل
```

### **❌ الحالة 3: البصمة غير متوفرة على الجهاز**

```
الزر يظهر: ❌ لا
السبب: الجهاز لا يدعم البصمة
الحل: استخدم تسجيل دخول يدوي
```

### **⚠️ الحالة 4: لا توجد بيانات محفوظة**

```
الزر يظهر: ✅ نعم
عند الضغط: رسالة "No saved credentials"
الخطوة: سجل دخول يدوياً أولاً
```

---

## 🔍 كيف تعرف حالة البصمة؟

### **من الإعدادات:**

```
Settings → Biometric Settings

سترى:
┌─────────────────────────────────┐
│ ✅ Biometric available: Yes     │
│ ✅ Biometric enabled: Yes/No    │
│                                 │
│ [Enable/Disable Biometric]      │
└─────────────────────────────────┘
```

### **من الكود:**

```dart
// التحقق من توفر البصمة
bool isAvailable = await BiometricService.isAvailable();

// التحقق من تفعيل البصمة
bool isEnabled = await SecureStorageService.isBiometricEnabled();

// التحقق من وجود بيانات
bool hasData = await SecureStorageService.hasSavedCredentials();
```

---

## 🎯 الخطوات التفصيلية

### **1. حفظ البيانات لأول مرة:**

```dart
// يتم تلقائياً بعد تسجيل الدخول الناجح
await SecureStorageService.saveCredentials(
  username: 'your_username',
  password: 'your_password',
);
```

### **2. تفعيل البصمة:**

```dart
// يتم تلقائياً بعد حفظ البيانات
await SecureStorageService.setBiometricEnabled(true);
```

### **3. تسجيل الدخول بالبصمة:**

```dart
// 1. التحقق من البصمة
bool authenticated = await BiometricService.authenticate(
  reason: 'Authenticate to access Jeel ERP',
);

// 2. إذا نجحت، جلب البيانات
if (authenticated) {
  final credentials = await SecureStorageService.getCredentials();
  // 3. ملء النموذج تلقائياً
  // 4. إرسال النموذج
}
```

---

## ❓ الأسئلة الشائعة

### **س: لماذا لا يظهر زر البصمة؟**

**ج:** تحقق من:
1. ✅ هل البصمة متوفرة على جهازك؟
2. ✅ هل البصمة مسجلة في إعدادات الهاتف؟
3. ✅ هل أنت في صفحة تسجيل الدخول؟

### **س: كيف أحذف البيانات المحفوظة؟**

**ج:** 
```
Settings → Manage Login Credentials → Delete Saved Data
```

### **س: كيف أعطل البصمة؟**

**ج:**
```
Settings → Biometric Settings → Disable Biometric
```

### **س: هل البيانات آمنة؟**

**ج:** ✅ نعم!
- البيانات مشفرة في Keychain (iOS) / Keystore (Android)
- لا يمكن الوصول إليها إلا عبر التطبيق
- محمية بمفاتيح التشفير الخاصة بالنظام

---

## 🎨 شكل زر البصمة

### **في صفحة تسجيل الدخول:**

```
┌─────────────────────────────────────┐
│                                     │
│  Username: [____________]           │
│  Password: [____________]           │
│                                     │
│                                     │
│     ┌─────────────────────────┐     │
│     │ 👆  Login with         │     │ ← زر البصمة
│     │     Biometric          │     │   (يظهر تلقائياً)
│     └─────────────────────────┘     │
│                                     │
└─────────────────────────────────────┘
```

### **حالة الزر:**

```
✅ يظهر: في صفحة تسجيل الدخول فقط
✅ الموقع: في المنتصف السفلي
✅ الحجم: كبير وواضح
✅ اللون: Gradient وردي (#A21955)
✅ الأنيميشن: نبض وحركة مستمرة
```

---

## 📝 ملخص سريع

### **لحفظ البصمة:**

1. ✅ سجل دخول يدوياً
2. ✅ وافق على حفظ البيانات
3. ✅ فعّل البصمة عند السؤال

### **لتسجيل الدخول بالبصمة:**

1. ✅ افتح صفحة تسجيل الدخول
2. ✅ اضغط زر البصمة 👆
3. ✅ ضع بصمتك
4. ✅ تسجيل دخول تلقائي! 🎉

---

## 🔐 الأمان

### **حماية البيانات:**

- ✅ مشفرة في Keychain/Keystore
- ✅ لا يمكن الوصول إليها إلا بعد البصمة
- ✅ محمية من التطبيقات الأخرى

### **حماية البصمة:**

- ✅ البصمة محلية على الجهاز
- ✅ لا يتم إرسالها لأي مكان
- ✅ آمنة تماماً

---

## 🎯 الخلاصة

**التطبيق يدعم البصمة بشكل كامل وآمن!**

- ✅ حفظ تلقائي للبيانات
- ✅ تفعيل سهل للبصمة
- ✅ تسجيل دخول سريع
- ✅ أمان عالي

**جرّب الآن واستمتع بالسهولة! 👆✨**

---

*آخر تحديث: 2024*

