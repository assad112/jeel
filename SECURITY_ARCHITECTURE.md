# 🏗️ هيكل الأمان - Security Architecture

## 📐 المخطط العام

```
┌─────────────────────────────────────────────────────────┐
│                   Flutter Application                    │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │         BankSecurityWrapper (Root Level)           │ │
│  │                                                    │ │
│  │  ┌──────────────────────────────────────────────┐ │ │
│  │  │     BiometricGateScreen (Auth Layer)        │ │ │
│  │  │                                              │ │ │
│  │  │  ┌────────────────────────────────────────┐ │ │ │
│  │  │  │      SplashScreen                      │ │ │ │
│  │  │  │                                        │ │ │ │
│  │  │  │  ┌──────────────────────────────────┐ │ │ │ │
│  │  │  │  │   WebViewScreen (App Content)   │ │ │ │ │
│  │  │  │  │                                  │ │ │ │ │
│  │  │  │  │   [User's Content Here]         │ │ │ │ │
│  │  │  │  └──────────────────────────────────┘ │ │ │ │
│  │  │  └────────────────────────────────────────┘ │ │ │
│  │  └──────────────────────────────────────────────┘ │ │
│  └────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 تدفق الأمان (Security Flow)

```
Start App
   │
   ↓
┌─────────────────────────────────────┐
│   BankSecurityWrapper Initialized   │ ← الطبقة الأولى
│   • Screenshot Protection: ON       │
│   • Jailbreak Check: Running        │
│   • Inactivity Timer: Started       │
└─────────────────────────────────────┘
   │
   ├─→ [Jailbreak Detected?] ──Yes──→ ❌ Show Error & Block
   │                                      "Device Compromised"
   │
   No
   │
   ↓
┌─────────────────────────────────────┐
│    BiometricGateScreen Loaded       │ ← الطبقة الثانية
│    • Check if Biometric Enabled     │
│    • Check if Credentials Saved     │
└─────────────────────────────────────┘
   │
   ├─→ [Has Credentials & Biometric ON?]
   │         │                 │
   │        Yes               No
   │         │                 │
   │         ↓                 │
   │   ┌─────────────┐         │
   │   │  Show       │         │
   │   │  Fingerprint│         │
   │   │  Prompt     │         │
   │   └─────────────┘         │
   │         │                 │
   │   [Auth Success?]         │
   │         │                 │
   │        Yes               No
   │         │                 │
   └─────────┴─────────────────┘
                 │
                 ↓
         ┌───────────────────┐
         │  SplashScreen     │ ← الطبقة الثالثة
         │  • Show Logo      │
         │  • Load Resources │
         └───────────────────┘
                 │
                 ↓
         ┌───────────────────┐
         │  WebViewScreen    │ ← الطبقة الرابعة
         │  • Auto-fill Form │   (المحتوى الرئيسي)
         │  • Monitor Login  │
         │  • User Interact  │
         └───────────────────┘
                 │
                 ↓
         [User Using App]
                 │
        ┌────────┴────────┐
        │                 │
   [5 min timeout]   [App to Background]
        │                 │
        ↓                 ↓
   Show Dialog       Show Blur Overlay
   "Session Timeout"  "Secured"
```

---

## 🧩 المكونات الرئيسية (Components)

### 1. BankSecurityWrapper
```
📦 BankSecurityWrapper
├── 🔒 Screenshot Protection
│   ├── protectDataLeakageOn()
│   └── preventScreenshotOn()
│
├── 🔍 Jailbreak Detection
│   ├── isDeviceCompromised()
│   └── performSecurityCheck()
│
├── ⏰ Inactivity Timer
│   ├── _startInactivityTimer()
│   ├── _resetInactivityTimer()
│   └── _showInactivityDialog()
│
└── 🎨 Lifecycle Observer
    ├── didChangeAppLifecycleState()
    ├── resumed → Hide Blur
    ├── paused → Show Blur
    └── inactive → Show Blur
```

### 2. SecurityService
```
🛡️ SecurityService
├── isDeviceCompromised()
│   └── Returns: bool
│
├── isDeveloperMode()
│   └── Returns: bool
│
├── isRealDevice()
│   └── Returns: bool
│
├── performSecurityCheck()
│   └── Returns: SecurityCheckResult
│       ├── isPassed: bool
│       ├── isCompromised: bool
│       ├── isRealDevice: bool
│       ├── errors: List<String>
│       └── warnings: List<String>
│
└── getDeviceSecurityInfo()
    └── Returns: Map<String, dynamic>
```

### 3. BiometricService (موجود مسبقاً)
```
👆 BiometricService
├── isAvailable()
├── authenticate()
└── getAvailableBiometrics()
```

### 4. SecureStorageService (موجود مسبقاً)
```
💾 SecureStorageService
├── saveCredentials()
├── getCredentials()
├── isBiometricEnabled()
└── deleteCredentials()
```

---

## 🔐 طبقات الحماية (Security Layers)

```
┌─────────────────────────────────────────────────┐
│  Layer 5: Inactivity Protection                │
│  • Monitor user activity                       │
│  • Auto-logout after timeout                   │
└─────────────────────────────────────────────────┘
                      ↑
┌─────────────────────────────────────────────────┐
│  Layer 4: Background Protection                │
│  • Hide content in app switcher                │
│  • Blur overlay when paused                    │
└─────────────────────────────────────────────────┘
                      ↑
┌─────────────────────────────────────────────────┐
│  Layer 3: Screenshot Prevention                │
│  • Block screenshots                           │
│  • Block screen recording                      │
└─────────────────────────────────────────────────┘
                      ↑
┌─────────────────────────────────────────────────┐
│  Layer 2: Biometric Authentication             │
│  • Fingerprint / Face ID                       │
│  • Secure credential storage                   │
└─────────────────────────────────────────────────┘
                      ↑
┌─────────────────────────────────────────────────┐
│  Layer 1: Device Integrity Check               │
│  • Jailbreak/Root detection                    │
│  • Developer mode check                        │
│  • Emulator detection                          │
└─────────────────────────────────────────────────┘
```

---

## 📊 تدفق البيانات (Data Flow)

### حفظ البيانات (Save Credentials)
```
User Login Success
      │
      ↓
┌──────────────────────┐
│  Capture Form Data   │
│  (username/password) │
└──────────────────────┘
      │
      ↓
┌──────────────────────┐
│  Show Dialog:        │
│  "Enable Biometric?" │
└──────────────────────┘
      │
      ├──→ User Clicks "Enable"
      │
      ↓
┌────────────────────────────────┐
│  SecureStorageService.save()   │
│  • Encrypted in Keychain/Store │
└────────────────────────────────┘
      │
      ↓
┌────────────────────────────────┐
│  setBiometricEnabled(true)     │
└────────────────────────────────┘
```

### استخدام البيانات (Use Credentials)
```
App Start
   │
   ↓
┌─────────────────────────────────┐
│  Check BankSecurityWrapper      │
│  ✓ Device is secure             │
└─────────────────────────────────┘
   │
   ↓
┌─────────────────────────────────┐
│  BiometricGateScreen            │
│  • hasSavedCredentials? → Yes   │
│  • isBiometricEnabled? → Yes    │
└─────────────────────────────────┘
   │
   ↓
┌─────────────────────────────────┐
│  Show Fingerprint Prompt        │
│  "Authenticate to access"       │
└─────────────────────────────────┘
   │
   ↓
┌─────────────────────────────────┐
│  User authenticates ✓           │
└─────────────────────────────────┘
   │
   ↓
┌─────────────────────────────────┐
│  Navigate to App                │
└─────────────────────────────────┘
   │
   ↓
┌─────────────────────────────────┐
│  WebViewScreen loads            │
│  • Check if login page          │
│  • Auto-fill credentials        │
│  • Submit form                  │
└─────────────────────────────────┘
   │
   ↓
┌─────────────────────────────────┐
│  User logged in automatically!  │
└─────────────────────────────────┘
```

---

## 🔄 دورة حياة الأمان (Security Lifecycle)

```
App Launch
   │
   ↓
[Initialize Security]
   │
   ├─→ Enable Screenshot Protection
   ├─→ Check Device Integrity
   ├─→ Start Inactivity Timer
   └─→ Add Lifecycle Observer
   │
   ↓
[App Running]
   │
   ├─→ [User Interacts] → Reset Timer
   │
   ├─→ [5 min No Interaction] → Show Warning
   │
   ├─→ [App to Background] → Show Blur
   │
   └─→ [App to Foreground] → Hide Blur
   │
   ↓
[Security Violation Detected?]
   │
   ├──Yes──→ Block App / Show Error
   │
   └──No───→ Continue Running
   │
   ↓
[App Closed]
   │
   └─→ Cleanup & Reset
```

---

## 🎯 نقاط القرار (Decision Points)

### 1. Jailbreak Check
```
┌─────────────────────────┐
│  Is Device Compromised? │
└─────────────────────────┘
         │
    ┌────┴────┐
   Yes       No
    │         │
    ↓         ↓
  Block     Allow
```

### 2. Biometric Check
```
┌──────────────────────────┐
│  Biometric Available?    │
└──────────────────────────┘
         │
    ┌────┴────┐
   Yes       No
    │         │
    ↓         ↓
  Show     Skip to
  Prompt    App
    │
    ↓
┌──────────────────────────┐
│  Authentication Success? │
└──────────────────────────┘
         │
    ┌────┴────┐
   Yes       No
    │         │
    ↓         ↓
  Allow    Retry/Skip
```

### 3. Inactivity Check
```
┌─────────────────────────┐
│  User Inactive 5 min?   │
└─────────────────────────┘
         │
    ┌────┴────┐
   Yes       No
    │         │
    ↓         ↓
  Show     Continue
  Dialog   Normally
```

---

## 📱 Platform-Specific Implementation

### Android
```
AndroidManifest.xml
├── USE_BIOMETRIC permission
├── USE_FINGERPRINT permission
└── Internet permissions

screen_protector
├── WindowManager.LayoutParams.FLAG_SECURE
└── Prevents screenshots

safe_device
└── Checks for root access
```

### iOS
```
Info.plist
├── NSFaceIDUsageDescription
└── Camera/Biometric permissions

screen_protector
├── UIScreen.captureProtection
└── Prevents screenshots

safe_device
└── Checks for jailbreak
```

---

## 🔧 Configuration Options

```dart
BankSecurityWrapper(
  // Feature Toggles
  enableScreenshotProtection: true,  // Default: true
  enableJailbreakDetection: true,    // Default: true
  enableInactivityTimeout: true,     // Default: true
  
  // Settings
  inactivityTimeout: Duration(minutes: 5),  // Default: 5 min
  showSecurityWarnings: true,               // Default: true
  
  // Callbacks
  onInactivityTimeout: () { },  // Optional
  onSecurityViolation: () { },  // Optional
  
  // Child
  child: YourApp(),
)
```

---

## 📊 مثال على السجل (Log Example)

```
🔒 Security Information:
══════════════════════════════════════════════════
  platform: Android
  version: 13
  sdk: 33
  brand: Samsung
  model: Galaxy S21
  isPhysicalDevice: true
  isCompromised: false
  isDeveloperMode: false
══════════════════════════════════════════════════
🔒 Screenshot protection enabled
📱 App resumed
⏰ Inactivity timeout reached
📱 App paused/inactive
```

---

## 🎓 Best Practices

1. **Always enable screenshot protection in production**
2. **Use reasonable inactivity timeout (3-5 minutes)**
3. **Handle security violations gracefully**
4. **Log security events for audit**
5. **Test on real devices, not emulators**
6. **Clear sensitive data on timeout**

---

**📐 Architecture Complete!**

هذا المخطط يوضح الهيكل الكامل لنظام الأمان المطبق في التطبيق.

